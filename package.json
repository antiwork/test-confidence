{
  "name": "test-confidence",
  "version": "0.2.0",
  "description": "Predictive Test Selection as a GitHub App. Cloudflare Worker, zero infra.",
  "main": "src/worker.ts",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {},
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20260424.0",
    "typescript": "^6.0.0",
    "vitest": "^3.1.3",
    "wrangler": "^4.14.0"
  }
}
